#include <stdio.h>

/**
 * main - prints its name, followed by a new line.
 * @argc: arguments count
 * @argv: aruments string
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
